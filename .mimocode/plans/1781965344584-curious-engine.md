# Plan: Clean FAISS outliers

## Что делаем
Удаляем 39 outliers (pnl > $0.12) и 1 убыточную запись (pnl < -$0.03) из FAISS.

## Файл
`ml_engine/scripts/clean_faiss.py` — уже существует, нужно запустить с правильными параметрами.

## Команда
```bash
docker exec ftg-ml-engine python3 /app/scripts/clean_faiss.py
```

Скрипт уже фильтрует: pnl > 0.15 → удалить,.pnl < -0.05 → удалить.
Нужно обновить порог: pnl > 0.12 → удалить.

## Верификация
1. Проверить что FAISS уменьшился с 200 до ~160
2. Перезапустить ml_engine
3. Проверить что модель использует чистые данные
