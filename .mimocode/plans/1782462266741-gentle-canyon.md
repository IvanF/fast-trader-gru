# План: снижение confidence до 0.25

## Изменения

### 1. `.env` (строки 17-18, 25)
```
CONFIDENCE_THRESHOLD=0.25      # было 0.30
KILL_CONFIDENCE_THRESHOLD=0.25  # было 0.30
LONG_CONFIDENCE_THRESHOLD=0.25  # было 0.45
```

### 2. `config.py` (строки 82, 83, 101)
```python
confidence_threshold=0.25      # было 0.50 (дефолт)
kill_confidence_threshold=0.25  # было 0.35
long_confidence_threshold=0.25  # было 0.50
```

### 3. Restart ml_engine

## Верификация
1. Backtest: `python scripts/backtest.py --symbol BTCUSDT,ETHUSDT,SOLUSDT --hours 168 --long-threshold 0.25 --short-threshold 0.25`
2. Compare с текущим: signal_rate, accuracy, PnL
3. Deploy → monitor: signals/min, online updates, pattern memory size
